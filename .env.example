# Secret URL token that unlocks the /orcamento quote builder (legacy share links).
ORCAMENTO_TOKEN=

# Password for the /admin login form.
ADMIN_PASSWORD=

# Key used to sign the admin session cookie (HMAC-SHA256).
# Generate one with: openssl rand -base64 32
SESSION_SECRET=
