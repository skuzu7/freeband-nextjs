/**
 * Decorative layer of drifting colored light beams that sit behind hero
 * content. Pure CSS (see `.stage-beams` in globals.css). Server component.
 */
export function StageBeams() {
  return <div className="stage-beams" aria-hidden />;
}
